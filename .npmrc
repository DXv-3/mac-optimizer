# ─── NPM Configuration — Mac Optimizer ───────────────────────────────────────
# Cage the npm cache to a local, user-owned directory.
# This prevents npm from ever writing to system-owned paths.
cache=./.npm-cache

# Never spawn privileged processes or change file ownership
unsafe-perm=false

# Strict SSL (always)
strict-ssl=true
